[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "nomeshops-sample"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["fastapi>=0.110", "uvicorn>=0.29", "numpy>=1.24"]
[tool.setuptools]
py-modules = ["app"]
